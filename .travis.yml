language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-generator-adfab-playground' ]; then cd .. && eval "mv $currentfolder generator-generator-adfab-playground" && cd generator-generator-adfab-playground; fi
